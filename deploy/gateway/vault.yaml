apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
  namespace: vault
spec:
  hostnames:
    - vault.localhost
  parentRefs:
    - name: local
      namespace: gateway
      sectionName: http
  rules:
    - backendRefs:
        - name: vault
          port: 8200